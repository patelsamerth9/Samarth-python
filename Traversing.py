x = 'hello world and how are you'
for i in range(len(x)):# Using range and len to traverse the string
    print(x[i])     # Printing each character at index i
    #with this method we can also access the index of each character easily same as the the array
    print(i)# Printing the index of each character(this line is only used to show index number and its optional)